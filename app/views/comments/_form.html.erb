<%= form_for([@topic, @topic.comments.build]) do |f| %>
  <p>
    <input type="hidden" name="topic[commenter]" value='<%= current_user.screen_name %>'>
    <%= f.label :your_thoughts %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
